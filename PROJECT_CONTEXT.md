# ISHDASIZ - Andijon Job Portal
## Полный контекст проекта для AI-ассистентов

---

## 🎯 О проекте

**ISHDASIZ** — это портал вакансий для Андижанской области Узбекистана, разработанный под эгидой местного хокимията (администрации). Название означает "БЕЗРАБОТНЫЙ" на узбекском.

**Цель:** Соединить соискателей работы с работодателями региона через современный веб-интерфейс с AI-ассистентом.

---

## 🛠 Технологический стек

| Категория | Технология |
|-----------|------------|
| **Фреймворк** | Next.js 15+ (App Router) |
| **Язык** | TypeScript 5 |
| **Стили** | Tailwind CSS 4 |
| **UI компоненты** | Radix UI, shadcn/ui |
| **База данных** | Supabase (PostgreSQL) |
| **Аутентификация** | SMS OTP через Eskiz.uz |
| **Карты** | Leaflet + react-leaflet |
| **AI** | OpenAI GPT-4o-mini |
| **Иконки** | Lucide React |
| **Пакетный менеджер** | pnpm |

---

## 📁 Структура проекта

```
ISHDASIZ NEW/
├── app/                          # Next.js App Router
│   ├── (public)/                 # Публичные страницы
│   │   ├── page.tsx              # Главная
│   │   ├── jobs/                 # Список вакансий
│   │   └── map/                  # Карта вакансий
│   ├── admin/                    # Админ-панель хокимията
│   │   ├── jobs/                 # CRUD вакансий
│   │   ├── applications/         # Заявки
│   │   └── stats/                # Статистика
│   ├── profile/
│   │   ├── job-seeker/           # Профиль соискателя
│   │   └── employer/             # Профиль работодателя
│   └── api/
│       ├── auth/                 # send-otp, verify-otp
│       ├── chat/                 # AI ассистент
│       └── jobs/                 # API вакансий
├── components/
│   ├── auth/                     # Модальное окно входа
│   ├── layout/                   # Header, Footer
│   └── ui/                       # shadcn компоненты
├── contexts/                     # React Context
│   ├── auth-modal-context.tsx    # Состояние модалки входа
│   ├── user-auth-context.tsx     # Сессия пользователя
│   └── language-context.tsx      # Мультиязычность (uz/ru)
├── hooks/
│   └── useAuth.ts                # Хук аутентификации
├── lib/
│   ├── supabase.ts               # Клиент Supabase
│   ├── eskiz.ts                  # SMS API
│   └── otp-store.ts              # Локальное хранение OTP (dev)
└── supabase/migrations/          # SQL миграции
```

---

## 🗄 База данных (Supabase)

### Основные таблицы:

```sql
-- Пользователи
users (id, phone, role ['job_seeker'|'employer'], is_verified, created_at)

-- Профили
job_seeker_profiles (id, user_id, full_name, city, about, ...)
employer_profiles (id, user_id, company_name, industry, ...)

-- Вакансии
jobs (id, title, description, salary_min, salary_max, district_id, category_id, ...)

-- OTP коды
otp_codes (id, phone, code VARCHAR(5), attempts, expires_at, verified)

-- Справочники
districts (id, name_uz, name_ru, type)
categories (id, name_uz, name_ru, icon)
```

---

## 🔐 Аутентификация

### Flow:
1. Пользователь нажимает "Kirish" → открывается модальное окно
2. Выбирает роль: Ish qidiruvchi (соискатель) / Ish beruvchi (работодатель)
3. Вводит номер телефона (+998 XX XXX-XX-XX)
4. Получает 5-значный SMS код через Eskiz.uz
5. Вводит код → создается/авторизуется пользователь
6. Редирект на профиль

### Dev режим:
- Без ENV переменных ESKIZ_EMAIL/ESKIZ_PASSWORD
- SMS не отправляется, код `12345` принимается
- Код выводится в console.log сервера

### Prod режим:
```env
ESKIZ_EMAIL=xasanjanov.x@gmail.com
ESKIZ_PASSWORD=API_ключ
ESKIZ_SMS_TEXT=ishdasiz.uz saytiga kirish uchun tasdiqlash kodi:
```

---

## 🌍 Мультиязычность

Три языка:
- `uz` — Ўзбекча (латиница)
- `uzCyrillic` — Ўзбекча (кириллица)  
- `ru` — Русский

Переключатель в шапке сайта.

---

## ✅ Текущее состояние (Январь 2026)

### Готово:
- [x] Публичные страницы (главная, вакансии, карта)
- [x] Админ-панель (CRUD вакансий)
- [x] Модальная аутентификация с SMS
- [x] 5-значные OTP коды
- [x] Dev режим без SMS
- [x] Профили job_seeker и employer
- [x] Сессия 30 дней
- [x] AI-ассистент в чате

### В процессе:
- [ ] Резюме соискателей
- [ ] Отклики на вакансии
- [ ] Мессенджер между соискателем и работодателем
- [ ] Верификация работодателей

---

## 📝 Как помогать с проектом

### При получении задачи:

1. **Спроси уточнения** если задача неясна
2. **Проверь существующий код** перед изменениями
3. **Используй существующие паттерны** (контексты, хуки, стили)
4. **Пиши на TypeScript** со строгой типизацией
5. **Tailwind CSS** для стилей, никакого vanilla CSS
6. **Тестируй сборку** после изменений: `npm run build`

### Важные файлы для понимания:

| Файл | Что делает |
|------|------------|
| `contexts/user-auth-context.tsx` | Сессия пользователя |
| `contexts/auth-modal-context.tsx` | Модальное окно входа |
| `lib/eskiz.ts` | SMS отправка |
| `app/api/auth/send-otp/route.ts` | API отправки кода |
| `app/api/auth/verify-otp/route.ts` | API проверки кода |
| `components/layout/header.tsx` | Шапка с навигацией |

### Стиль кода:

```tsx
// Компоненты — функциональные с хуками
'use client'; // Для клиентских компонентов

export function MyComponent() {
    const { lang } = useLanguage();
    const { user, isAuthenticated } = useUserAuth();
    
    return (
        <div className="bg-white rounded-xl shadow-lg p-6">
            {/* Tailwind классы */}
        </div>
    );
}
```

---

## 🚀 Запуск проекта

```bash
cd "c:\Users\user\Desktop\ISHDASIZ NEW"
pnpm install
pnpm dev          # Разработка на http://localhost:3000
pnpm build        # Продакшен сборка
```

---

## 📞 Контакты / Владелец

- Проект для: Хокимият Андижанской области
- Разработчик: xasanjanov.x@gmail.com

---

## 💡 Примеры хороших промтов для этого проекта

### Для добавления функций:
```
Добавь страницу создания резюме для соискателя.
Таблица resumes уже есть в Supabase.
Поля: title, desired_position, desired_salary_min/max, experience (JSONB), skills (TEXT[]).
Используй существующий стиль профиля.
```

### Для исправления багов:
```
При входе показывает ошибку "Kod topilmadi".
Проверь:
1. Таблица otp_codes существует?
2. Код сохраняется в базу?
3. Логи в терминале сервера?
```

### Для стилизации:
```
Сделай карточки вакансий более современными:
- Добавь градиентную тень
- Hover эффект с scale
- Иконки категорий
Используй Tailwind.
```
