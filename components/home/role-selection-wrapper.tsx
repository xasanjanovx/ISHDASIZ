'use client';

/**
 * Role Selection Wrapper
 * 
 * This component was previously used for role selection modal flow.
 * Now the role selection is handled by AuthModal.
 * 
 * Keeping this as a placeholder to avoid breaking the layout import.
 */
export function RoleSelectionWrapper() {
    // Auth and role selection now handled by AuthModal
    return null;
}
